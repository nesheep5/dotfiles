"=============================
"  .gvimrc for MacVim
"=============================
set columns=120
set lines=55

" カラースキーマ
" colorscheme hybrid
" colorscheme codeschool
" colorscheme railscasts
 colorscheme Tomorrow-Night
" let g:seoul256_background = 233
" colo seoul256

" コマンドラインに使われる画面上の行数。
set cmdheight=2

" タブバー表示 (0:常に非表示 1:複数タブの場合表示 2:常に表示)
set showtabline=2

" Visual選択で選択されたテキストが、自動的にクリップボードレジスタ "*" にコピーされる。
set guioptions+=a
